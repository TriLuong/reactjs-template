export enum ROUTE {
  AUTHENTICATED = "/dashboard",

  UN_AUTHENTICATED = "/login",

  HOME = "/home",
  LOGIN = "/login",
  VERIFY_EMAIL = "/verify-email",
  SIGN_IN = "/sign-in",
  SIGN_UP = "/signup",
}
